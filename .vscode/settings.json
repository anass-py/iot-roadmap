{
  // Pointe VSCode sur le venv du projet, pas sur le python système.
  // Sans ça, le bouton ▶ lance /Library/Frameworks/... et `import iot` échoue.
  "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python",
  "python.terminal.activateEnvironment": true
}
