[project]
name = "iot"
version = "0.1.0"
description = "Pipeline IoT : capteurs MQTT -> TimescaleDB -> API FastAPI -> alertes"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
    "paho-mqtt",
    "psycopg[binary]",
    "pydantic",
    "python-dotenv",
    "requests",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
